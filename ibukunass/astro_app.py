# AstroBank App:
# This app is used to transfer,withdraw and deposit money.
# Transactions can take place on the app once you've sucessfully signed up on the app, which requires name,date of birth,bvn and pin.
# In Python, while True creates an infinite loop, which is a loop that runs indefinitely.

        
while True:
            user_input = input("Enter 'hi' to start page: ")
            if  user_input == 'hi':
                print("Welcome, how may i help you?")
                break
            else:
                print("ERROR")
    
